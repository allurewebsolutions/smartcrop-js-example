This is a simplified example of a Smartcrop.js.

Smartcrop only works with local images.

### To initiate (Must have Node.js installed)
* `git clone https://github.com/allurewebsolutions/smartcrop-js-example.git .`
* `npm install`
* `grunt`